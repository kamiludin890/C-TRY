using System;

namespace MyApplication
{
  class Program
  {
    static void Main(string[] args)
    {
      string first;
      string last;
      string name = first + last;
      Console.ReadLine("First Name");
      Console.WriteLine("First Name"+ first);
      Console.WriteLine(name);
      Console.ReadKey("Click any button");
    }
  }
}